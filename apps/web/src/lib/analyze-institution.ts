/**
 * Institutional research visibility analysis — re-exported from the shared
 * core library (`@nexus-score/core`) so the web app and the MCP server run
 * the exact same evidence-based Crossref classification.
 * (Filename kept to avoid churn across existing importers.)
 */
export { analyzeInstitution, searchInstitutions } from '@nexus-score/core';
